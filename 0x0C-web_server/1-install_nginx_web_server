#!/usr/bin/env bash
# This script sets up an nginx server on an Ubuntu machine and configures it to serve a custom HTML page with "Hello World!"


# Update package lists and install Nginx
apt-get update -y
apt-get install nginx -y

# Configure Nginx to listen on port 80
sed -i 's/listen 80 default_server;/listen 80 default_server;/g' /etc/nginx/sites-available/default
sed -i 's/listen \[::\]:80 default_server;/listen \[::\]:80 default_server;/g' /etc/nginx/sites-available/default

# Configure Nginx to serve custom HTML content
echo "Hello World!" > /var/www/html/index.nginx-debian.html

# Start Nginx without using systemctl
service nginx restart
